const $=id=>document.getElementById(id);const FIXED_PAGES=40,FIXED_PER_PAGE=250,HISTORICAL_MAX_COINS=250,MAX_HISTORY_DAYS=365;let catalog=null,downloadName='cryptos.json',stopRequested=false,activeController=null;const fmt=n=>new Intl.NumberFormat('pt-BR').format(n);const policy=globalThis.CollectionPolicy||{MIN_PAGE_DELAY_SECONDS:5,BLOCK_DELAY_SECONDS:15,PAGES_PER_BLOCK:4,MAX_ATTEMPTS:4,getRetryDelaySeconds:v=>Math.max(60,Number(v)||60)};const {MIN_PAGE_DELAY_SECONDS:PAGE_DELAY,BLOCK_DELAY_SECONDS:BLOCK_DELAY,PAGES_PER_BLOCK,MAX_ATTEMPTS:MAX_RETRIES}=policy;
function show(id){['empty','progressCard','results','error'].forEach(x=>$(x).classList.toggle('hidden',x!==id))}
const monthNames=['janeiro','fevereiro','março','abril','maio','junho','julho','agosto','setembro','outubro','novembro','dezembro'];
function monthFileName(date){const d=new Date(date+'T00:00:00Z');return (monthNames[d.getUTCMonth()]+d.getUTCFullYear()).normalize('NFD').replace(/[\u0300-\u036f]/g,'')+'.json'}
function populateHistoryMonths(){const select=$('historyMonth'),now=new Date(),today=Date.UTC(now.getUTCFullYear(),now.getUTCMonth(),now.getUTCDate());select.innerHTML='';for(let offset=1;offset<=24;offset++){const end=new Date(Date.UTC(now.getUTCFullYear(),now.getUTCMonth()-offset+1,0));const age=Math.floor((today-end.getTime())/86400000);if(age>MAX_HISTORY_DAYS)break;const value=end.toISOString().slice(0,10),option=document.createElement('option');option.value=value;option.textContent=monthNames[end.getUTCMonth()][0].toUpperCase()+monthNames[end.getUTCMonth()].slice(1)+' de '+end.getUTCFullYear();select.appendChild(option)}}
function selectedMode(){return document.querySelector('input[name="catalogMode"]:checked').value}
function syncMode(){const historical=selectedMode()==='historical';$('monthField').classList.toggle('hidden',!historical);$('historyNotice').classList.toggle('hidden',!historical);$('limit').innerHTML=historical?'1 página × 250 criptos<br>até 250 ativos':'40 páginas × 250 criptos<br>até 10.000 ativos'}
populateHistoryMonths();document.querySelectorAll('input[name="catalogMode"]').forEach(input=>input.addEventListener('change',syncMode));syncMode();function formatTime(sec){sec=Math.max(0,Math.ceil(sec));const h=Math.floor(sec/3600),m=Math.floor(sec%3600/60),s=sec%60;return h?`${h}h ${String(m).padStart(2,'0')}m ${String(s).padStart(2,'0')}s`:m?`${m}m ${String(s).padStart(2,'0')}s`:`${s}s`}
function estimatedWaitAfter(page,pages){let total=0;for(let p=page;p<pages;p++)total+=p%PAGES_PER_BLOCK===0?BLOCK_DELAY:PAGE_DELAY;return total}
function progress(page,pages,count,status,msg,title){const pct=Math.round(page/pages*100);$('progressPct').textContent=`${pct}%`;$('progressBar').style.width=`${pct}%`;$('statPages').textContent=String(page);$('statCoins').textContent=fmt(count);$('progressTitle').textContent=title||(page?`Página ${page} de ${pages}`:'Preparando coleta…');$('progressMessage').textContent=msg||''}
function sleepCountdown(seconds,pagesDone,pages){return new Promise(resolve=>{let left=Math.ceil(seconds);const tick=()=>{if(stopRequested){$('nextPageTimer').textContent='Parado';$('totalTimer').textContent='Parado';return resolve(false)}$('nextPageTimer').textContent=formatTime(left);$('totalTimer').textContent=formatTime(left+estimatedWaitAfter(pagesDone+1,pages));if(left<=0)return resolve(true);left--;setTimeout(tick,1000)};tick()})}
async function fetchWithTimeout(url,timeoutMs=25000){activeController=new AbortController();const timer=setTimeout(()=>activeController.abort(),timeoutMs);try{return await fetch(url,{signal:activeController.signal,cache:'no-store'})}catch(error){if(stopRequested&&error?.name==='AbortError')return null;if(error?.name==='AbortError')throw new Error('A consulta demorou mais de 25 segundos para responder.');throw new Error(`Falha de conexão com o servidor: ${error?.message||'erro desconhecido'}`)}finally{clearTimeout(timer);activeController=null}}
async function fetchPage(page,currency,perPage,pages,rows){const u=new URL('/api/markets',location.origin);u.searchParams.set('page',page);u.searchParams.set('currency',currency);u.searchParams.set('per_page',perPage);u.searchParams.set('_',Date.now());for(let attempt=1;attempt<=MAX_RETRIES;attempt++){if(stopRequested)return null;const r=await fetchWithTimeout(u);if(!r)return null;let data;try{data=await r.json()}catch{data={}}if(r.ok){if(!Array.isArray(data))throw new Error('A CoinGecko retornou dados em formato inesperado.');return data}const upstreamStatus=Number(data.upstream_status??data.status??r.status);if(r.status!==429||attempt===MAX_RETRIES)throw new Error((data.error||'A CoinGecko recusou a consulta.')+' (HTTP '+upstreamStatus+')');const wait=policy.getRetryDelaySeconds(data.retry_after_seconds??r.headers.get('Retry-After'));progress(page-1,pages,rows.length,'Limite atingido',`CoinGecko respondeu 429. Nova tentativa em ${formatTime(wait)}.`,'Aguardando CoinGecko');const go=await sleepCountdown(wait,page-1,pages);if(!go)return null}return null}
async function fetchHistory(coin,date,currency,index,total,rows){const u=new URL('/api/history',location.origin);u.searchParams.set('id',coin.id);u.searchParams.set('date',date);u.searchParams.set('currency',currency);u.searchParams.set('_',Date.now());for(let attempt=1;attempt<=MAX_RETRIES;attempt++){if(stopRequested)return null;const r=await fetchWithTimeout(u);if(!r)return null;let data;try{data=await r.json()}catch{data={}}if(r.ok)return {...coin,...data,market_cap_rank:null,price_change_percentage_24h:null,last_updated:date};if(r.status===404)return null;if(r.status!==429||attempt===MAX_RETRIES)throw new Error((data.error||'A CoinGecko recusou a consulta histórica.')+' (HTTP '+(data.upstream_status||r.status)+')');const wait=policy.getRetryDelaySeconds(data.retry_after_seconds??r.headers.get('Retry-After'));progress(index-1,total,rows.length,'Limite atingido',`CoinGecko respondeu 429. Nova tentativa em ${formatTime(wait)}.`,'Aguardando CoinGecko');const go=await sleepCountdown(wait,index-1,total);if(!go)return null}return null}
function build(rows,currency,stopped=false){return{generated_at:new Date().toISOString(),last_updated_timestamp:new Date().toISOString(),source:'CoinGecko',vs_currency:currency,total:rows.length,collection_status:stopped?'stopped':'completed',historical_date:rows[0]?.historical_date||null,cryptos:rows.map(c=>({id:c.id,symbol:(c.symbol||'').toUpperCase(),name:c.name,image:c.image,current_price:c.current_price,market_cap:c.market_cap,market_cap_rank:c.market_cap_rank,price_change_percentage_24h:c.price_change_percentage_24h,last_updated:c.last_updated}))}}
function finishPartial(rows,currency){if(!rows.length){show('error');$('error').textContent='Coleta interrompida antes de concluir a primeira página. Nenhum dado disponível para download.';return}catalog=build(rows,currency,true);render();show('results');$('countLabel').textContent=`Coleta interrompida • ${fmt(rows.length)} registros disponíveis para download`}
$('stop').addEventListener('click',()=>{stopRequested=true;$('stop').disabled=true;$('stop').textContent='Parando…';if(activeController)activeController.abort()});
$('generate').addEventListener('click',async()=>{const btn=$('generate'),mode=selectedMode(),historical=mode==='historical';stopRequested=false;downloadName=historical?monthFileName($('historyMonth').value):'cryptos.json';$('stop').disabled=false;$('stop').textContent='■ Parar coleta';btn.disabled=true;$('error').classList.add('hidden');show('progressCard');const pages=historical?1:FIXED_PAGES,per=FIXED_PER_PAGE,currency=$('currency').value;let rows=[];$('nextPageTimer').textContent='Consultando…';$('totalTimer').textContent=formatTime(estimatedWaitAfter(0,pages));try{for(let p=1;p<=pages;p++){if(stopRequested)break;progress(p-1,pages,rows.length,'Consultando',`Enviando solicitação da página ${p}…`,`Consultando página ${p} de ${pages}`);$('nextPageTimer').textContent='Consultando…';await new Promise(requestAnimationFrame);const data=await fetchPage(p,currency,per,pages,rows);if(!data||stopRequested)break;rows.push(...data);if(p<pages){const wait=p%PAGES_PER_BLOCK===0?BLOCK_DELAY:PAGE_DELAY;progress(p,pages,rows.length,'Aguardando','',`Página ${p} de ${pages} concluída`);const go=await sleepCountdown(wait,p,pages);if(!go)break}}
if(historical&&!stopRequested){const candidates=rows.slice(0,HISTORICAL_MAX_COINS),date=$('historyMonth').value,historicalRows=[];for(let i=0;i<candidates.length;i++){if(stopRequested)break;progress(i,candidates.length,historicalRows.length,'Consultando histórico',`Preço de ${candidates[i].name} no fechamento selecionado…`,`Ativo ${i+1} de ${candidates.length}`);const item=await fetchHistory(candidates[i],date,currency,i+1,candidates.length,historicalRows);if(item)historicalRows.push(item);if(i<candidates.length-1&&!stopRequested){const wait=(i+1)%PAGES_PER_BLOCK===0?BLOCK_DELAY:PAGE_DELAY;const go=await sleepCountdown(wait,i+1,candidates.length);if(!go)break}}rows=historicalRows.sort((a,b)=>(b.market_cap||0)-(a.market_cap||0)).map((row,index)=>({...row,market_cap_rank:index+1}))}
if(stopRequested){finishPartial(rows,currency);return}catalog=build(rows,currency);render();show('results');$('nextPageTimer').textContent='Concluído';$('totalTimer').textContent='0s'}catch(e){if(stopRequested)finishPartial(rows,currency);else{show('error');$('error').textContent=`Não foi possível gerar o catálogo: ${e?.message||String(e)}`}}finally{btn.disabled=false;$('stop').disabled=false}});
function price(v,c){if(v==null)return'—';return new Intl.NumberFormat('pt-BR',{style:'currency',currency:c.toUpperCase(),maximumFractionDigits:v<1?8:2}).format(v)}function render(){const rows=catalog.cryptos;$('totalMetric').textContent=fmt(catalog.total);$('updatedMetric').textContent=new Date(catalog.generated_at).toLocaleString('pt-BR',{dateStyle:'short',timeStyle:'short'});$('currencyMetric').textContent=catalog.vs_currency.toUpperCase();renderRows(rows)}function renderRows(rows){const c=catalog.vs_currency;$('tbody').innerHTML=rows.slice(0,1000).map(r=>`<tr><td>${r.market_cap_rank??'—'}</td><td><div class="coin"><img src="${r.image||''}" alt="" loading="lazy"><div><b>${esc(r.name)}</b><span>${esc(r.symbol)}</span></div></div></td><td>${price(r.current_price,c)}</td><td class="${(r.price_change_percentage_24h??0)>=0?'positive':'negative'}">${r.price_change_percentage_24h==null?'—':r.price_change_percentage_24h.toFixed(2)+'%'}</td><td>${esc(r.id)}</td></tr>`).join('');$('countLabel').textContent=`Exibindo ${fmt(rows.length)} de ${fmt(catalog.total)} registros`}
function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}$('search').addEventListener('input',e=>{if(!catalog)return;const q=e.target.value.trim().toLowerCase();renderRows(!q?catalog.cryptos:catalog.cryptos.filter(r=>r.name.toLowerCase().includes(q)||r.symbol.toLowerCase().includes(q)||r.id.toLowerCase().includes(q)))});$('download').addEventListener('click',()=>{if(!catalog)return;const blob=new Blob([JSON.stringify(catalog,null,2)],{type:'application/json;charset=utf-8'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=downloadName;a.click();URL.revokeObjectURL(a.href)});