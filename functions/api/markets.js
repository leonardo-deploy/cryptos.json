const json=(data,status=200)=>new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff'}});
export async function onRequestGet(context){try{const url=new URL(context.request.url);const currency=(url.searchParams.get('currency')||'brl').toLowerCase();const page=Math.max(1,Math.min(80,Number(url.searchParams.get('page')||1)));const perPage=Math.max(1,Math.min(250,Number(url.searchParams.get('per_page')||250)));if(!['brl','usd','eur'].includes(currency))return json({error:'Moeda inválida.'},400);const endpoint=new URL('https://api.coingecko.com/api/v3/coins/markets');endpoint.searchParams.set('vs_currency',currency);endpoint.searchParams.set('order','market_cap_desc');endpoint.searchParams.set('per_page',String(perPage));endpoint.searchParams.set('page',String(page));endpoint.searchParams.set('sparkline','false');endpoint.searchParams.set('price_change_percentage','24h');const supplied=context.request.headers.get('X-CoinGecko-Key');const secret=context.env.COINGECKO_API_KEY;const headers={accept:'application/json'};const key=supplied||secret;if(key)headers['x-cg-demo-api-key']=key;const response=await fetch(endpoint,{headers});if(!response.ok){let detail='A CoinGecko recusou a consulta.';if(response.status===429)detail='Limite de requisições da CoinGecko atingido. Aguarde e tente novamente ou informe uma chave Demo.';return json({error:detail,status:response.status},response.status)}return new Response(response.body,{status:200,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff'}})}catch(error){console.error(JSON.stringify({event:'coingecko_proxy_error',message:error instanceof Error?error.message:String(error)}));return json({error:'Falha temporária ao consultar os dados de mercado.'},502)}}
export function onRequestOptions(){return new Response(null,{status:204,headers:{allow:'GET, OPTIONS'}})}